<% if @errors %>
  <ul>
    <% @errors.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>
<form action="/client/products" method="post">
<%= form_tag "/client/products/#{}{@product_id}", method: :post do %>
  <div>
    Name: <input type="text" name="name" value="<%= @product['name'] %>" />
  </div>
  <div>
    Price: <input type="text" name="price" value="<%= @product['price'] %>" />
  </div>
  <div>
    Description: <input type="text" name="description" value="<%= @product['description'] %>" />
  </div>
  <div>
    Supplier ID: <input type="text" name="supplier_id" value="<%= @product['supplier_id'] %>">
  </div>
  <div>
    Image URL: <input type="text" name="image_url" value="<%= @product['image_url'] %>" />
  </div>
  <input type="submit" name="" value="Create Product" >
<% end %>